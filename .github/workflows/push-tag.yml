name: "Create and push release tag"

on:
  workflow_dispatch:
  schedule:
    "0 10 * * tue/2"

jobs:
  tagging:
    runs-on: ubuntu-latest

    steps:
      - name: Upstream tag
        uses: ochosi/composite-actions@v2
        with:
          token: "${{ secrets.TOKEN }}"